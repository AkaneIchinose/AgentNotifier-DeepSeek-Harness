# Agent 提醒器（AgentNotifier）需求文档 v1.5

> 状态：M1 ✅（2026-08-14）、M2 ✅、M3 ✅、M4 ✅（应用内富通知弹窗 v0.3）。本文档为开发依据。

## 0. 需求确认记录

| 决策点 | 决定 |
|---|---|
| 覆盖范围 | Claude Code + opencode（hooks）✅；DSH（WebSocket 事件流监听，零配置）✅ |
| 接入方式 | hooks 自动写入 + 备份/预览/回滚 + uninstall.ps1 一键恢复 ✅ |
| 通知方式 | 系统 Toast（WinRT via PowerShell，零 NuGet）✅，失败回退原生气球 |
| 内置音频 | 合成 + Windows 经典（8 款）✅；自定义导入 WAV/MP3 ✅；flac/ogg 待解码器 |
| 勿扰/静音/去抖 | ✅ |
| 技术栈 | .NET 8 + WPF 绿色 exe ✅；单文件发布脚本（需网络） |
| 界面 | 中文五页 UI ✅、浅蓝主题 ✅、**深色模式（跟随系统/手动）✅**、应用图标 ✅ |
| 开发日志/项目文档 | ✅ 持续更新 |

## 1. 事件采集（三端）

- Claude Code：hooks Notification（需要介入）/ Stop（完成）✅
- opencode：hooks notification / session.idle ✅
- DSH：DshMonitor 连官方 WebSocket（/api/events.mux + /api/events.host）：question/requested、approval/requested → 需要介入；host/session-status running 下降沿 → 完成；3 秒自动重连；概览页状态显示 ✅

## 2. 音频

- 内置 8 款；两种事件独立音效/音量/次数/间隔；去抖；静音；
- 自定义导入：WAV(PCM) + **MP3（MediaPlayer/MediaFoundation 播放，独立 STA 线程）** ✅；flac/ogg 待支持。

## 3. 通知

- 系统 Toast（WinRT）：toast.ps1（环境变量传参防注入；开始菜单快捷方式 AUMID，容错回退 appId）；失败静默回退气球 ✅
- **M4 应用内富通知弹窗（v0.3，默认）**：
  - 彩色类型徽标：选择（蓝）/ 权限（琥珀）/ 提交结果（绿）/ 通用按事件类型
  - 来源 Agent 标注（DSH 会话短 ID / 工具名）+ 会话 ID + 时间页脚
  - 自定义顶部图片（绝对或相对数据目录路径）+ 自定义内容模板（{agent} {tool} {type} {session} {summary} {time} {title}）
  - 右下角堆叠、滑入/淡出动画、自动关闭（3-30s）、点击打开主窗口
  - 通知样式三选一：应用内弹窗 / 系统 Toast / 原生气泡（通知页可切换 + 预览按钮）
  - 事件模型扩展：MsgType（choice/permission/result/generic）+ Agent 字段 + Model 字段；HTTP API 向后兼容（可选 msg/agent/model）✅
  - **模型样式（v0.3，软件端自动识别）**：
    - DshMonitor 周期性调用本机 RPC（session.list + session.models）自动识别每个 DSH 会话的模型 ID、
      目录显示名（如 DeepSeek-V4-Flash）与会话标题，新模型自动加入配置列表（无需手动填关键词）✅
    - 每个模型完全自定义：显示名 / 颜色 / 横幅图片 / 弹窗标题模板 / 回复内容模板（占位符
      {agent} {tool} {type} {model} {modelName} {session} {summary} {time} {title}）；
      弹窗按来源模型套用对应标题、内容、图片与颜色，模型徽标显示友好显示名 ✅
    - 通知页最上层为"选择模型"下拉 + 该模型的全部编辑项 + 预览/删除；未匹配模型回退全局默认内容 ✅
    - 匹配优先级：ModelId 精确 → Name 子串（兼容旧关键词配置如 flash/claude）✅
  - **DPI 修复（v0.3）**：app.manifest 声明 PerMonitorV2；弹窗阴影边距 22px + 缩小模糊，
    修复高 DPI 下阴影被窗口矩形裁剪出的灰色直角与文字模糊 ✅
- Win10 可在系统设置调通知位置；Win11 固定右下角。

## 4. 常驻与 UI

- 托盘、单实例、自启、勿扰、事件日志、崩溃日志（crash.log）✅
- 五页中文 UI、浅蓝主题 #4FC3F7、Win11 亚克力标题栏、**深色模式**（system/light/dark，动态换色字典）✅
- 应用图标（7 尺寸）✅

## 5. 技术方案

- .NET 8 + WPF，零外部 NuGet 依赖（离线构建）；
- EventServer（TcpListener HTTP）+ DshMonitor（ClientWebSocket）+ ReminderEngine + AudioPlayer（WAV 自研编解码 + MP3 MediaPlayer）+ Toast（WinRT via PS5.1）；
- 主题：LightColors/DarkColors.xaml 动态替换，全 DynamicResource；
- 发布：dist/AgentNotifier 绿色目录；scripts/publish.ps1 单文件（需网络）。

## 6. 测试（全绿）

Smoke 全量断言：服务/ping/鉴权/去抖/静音/勿扰/音效解码/播放（WAV+MediaPlayer）/notify.ps1 e2e/toast.ps1 WinRT/DSH WebSocket 实测/MP3 导入。

## 7. 里程碑

- M1 ✅；M2 ✅；M3：mp3 ✅、深色 ✅、图标 ✅、崩溃日志 ✅；剩余：flac/ogg、单文件发布（需网络）、用户手册完善、安装包与签名（可选）。

## 8. 已知限制

- Win11 通知固定右下角；flac/ogg 暂不支持；单文件发布需网络；DSH 事件语义以官方 rc 版为准；
- **opencode 1.x 配置不支持 hooks（官方已移除，2.x 恢复，键名 hook）**：App 接入向导已做版本检测，opencode 需 2.x 才能接入提醒；升级命令 opencode upgrade。